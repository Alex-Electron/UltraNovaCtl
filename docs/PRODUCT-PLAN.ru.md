# Самостоятельный редактор и библиотекарь UltraNova

Требования уточнены с Александром 2026-09-08; статус сверён 2026-09-11.
Это целевое поведение продукта, а не описание уже реализованных функций.
Текущие исправления и порядок продолжения: [проверка ядра](EDITOR-REVIEW-2026-09-11.ru.md).
Границы ещё незавершённой описи: [реестр функций](NATIVE-EDITOR-INVENTORY.ru.md).
Первый проверенный срез требований: [Oscillator/Filter по PDF, XML и VST](OSC-FILTER-REQUIREMENTS.ru.md).

## Основная задача

Одно настольное приложение с тремя разделами: **Редактор**, **Библиотека**, **Automap**.
Редактирование звука и работа с библиотекой доступны без DAW и без загрузки VST.
Звук создаёт подключённая UltraNova; приложение управляет аппаратным синтезатором.
Automap сохраняется как самостоятельная функция управления панелью и отправки MIDI в DAW.

Совместимость с официальными UltraNova Editor, Librarian и Automap — условие приёмки.
Запуск Ableton с родным VST должен оставлять родному редактору возможность управлять
синтезом. Закрывать наше приложение ради этого не требуется.

## Редактор

- Разделы по устройству звука: осцилляторы/микшер, фильтры, огибающие/LFO,
  матрица модуляции, эффекты, арпеджиатор/вокодер, Tweak/Touch.
- Осмысленные названия, единицы измерения и списки значений вместо номеров CC/NRPN.
- Чтение текущего патча и отображение изменений, сделанных на панели синтезатора.
  Канал читается из байта 13 статуса (значение 0–15; на стенде был канал 2).
  Синхронизация — уведомления и отложенный запрос свежего дампа: прибор объявляет выбор патча
  (Program/Bank/NRPN), правки параметров (NRPN 62,x) и отдаёт патч целиком 526-байтным
  дампом. **Канал для этого найден и проверен: приватная пара Port 1, пин 6 на чтение и
  пин 10 на запись** (`docs/HIDI-AND-PORTS.ru.md`). При `MIDI Out = Off` она даёт всё
  нужное: патч по запросу `0x40`, статус по `0x60`, объявление смены патча, новый дамп
  после смены, живые правки параметров через `CC`/`NRPN` на канале 2 и имя патча в
  смещениях 2–17. Ни публичный порт, ни `MIDI Out`, ни `Hidi64.dll` не нужны.
- Изменения ручек применяются к текущему звуку; входящие изменения обновляют интерфейс
  без обратной отправки и MIDI-петли.
- **У пина 6 один читатель.** Движок уже открывает его ради клавиатуры; редактор не
  открывает его повторно, а получает сообщения от того же читателя с разбором по типу:
  ноты и колёса в пересылку, `SysEx`-дампы и `CC`/`NRPN` канала 2 в модель патча. Запись
  редактора идёт в пин 10, который сейчас не занят никем.
- Источник истины — **опрос** (`0x40`), объявления и поток `CC` дают мгновенную реакцию
  между опросами. Так делает и родной плагин: у него на это `SetTimer` с запросом через
  секунду после Program Change.
- Undo/Redo и сравнение с исходным патчем. Локальный черновик сохраняется независимо
  от подключения к прибору и текущего владельца аппаратного редактора.
- Явное различие между сохранением в библиотеку компьютера и записью в слот синтезатора.

### Планка: лучше родного редактора и **ни одной потерянной функции**

Требование владельца, зафиксировано 09.09.2026. Интерфейс должен быть удобнее, чем у
родного `UltraNova Editor`, но полнота — не предмет компромисса: всё, что умеет родной
плагин, должно уметься и здесь.

Отсюда порядок работ: **сначала полная опись возможностей родного плагина**, потом дизайн.
Опись — не пересказ отчёта, а перечень, по которому можно ставить галочки, и делать её надо
по живым файлам, а не по памяти. Что для этого уже лежит на диске:

| | |
|---|---|
| `C:\Program Files\VSTPlugIns\UltraNova Editor 64.dll` | 970 240 байт, 23.12.2014 — сам плагин |
| `…\UltraNova Editor 64 (Automap).dll` | 155 648 байт — обёртка Automap, своего MIDI не имеет |
| `C:\Program Files\Novation\UltraNova\Resources\UltraNovaLayout.xml` | вся раскладка интерфейса: страницы, секции, 1345 контролов |
| `…\Resources\InitPatch.syx`, `UNOS.syx` | начальный патч и образ прошивки |
| `…\Resources\Statics.nun`, `Assets.nun` | заводские базы патчей |
| `research-patch-editor/agent-findings/editor-native-notes.md` | подробный разбор плагина, 32 КБ |
| `research-patch-editor/audit/decompiled-native/` | декомпиляция Interop, WinLibrarian, AutomapServer, Odyssey |

### Модель параметров уже извлечена — и она полнее, чем считалось

`research-patch-editor/tables/ultranova-layout-params.json` — 497 записей, и в каждой ровно
то, что нужно редактору:

* имя и короткое имя, тип (`int` 293, `enum` 166, `bool` 36, `double` 2), единицы,
  минимум/максимум/шаг/значение по умолчанию;
* **MIDI-адрес** — у 495 из 497;
* **смещение внутри патча** — у 454;
* **списки значений с названиями** у всех 166 перечислимых: например, Osc1 Waveform — это
  72 названных значения, а не «0..71».

454 параметра имеют адрес и смещение одновременно, то есть живая правка и работа с файлом
адресуются одним и тем же описанием. Это база для модели, а не обещание 497 независимых
аппаратных регуляторов: часть записей служебные.

**Откуда таблица и почему её нет в репозитории.** Она извлечена из
`UltraNovaLayout.xml` — ресурсного файла установленного софта Novation, то есть это
производная от чужих данных. Публичная карта MIDI из руководства (`mi-parsed.json`) даёт
только 128 CC без NRPN и на 497 параметров не тянет. Поэтому таблица лежит в
`research-patch-editor/tables/` и в публичный репозиторий **не попадает**, как и вся эта папка.

Решение для продукта: **таблица строится на машине пользователя из его же установки
Novation** — небольшой извлекатель читает локальный `UltraNovaLayout.xml` и складывает JSON
рядом с настройками. Каждый получает таблицу из собственной копии, мы ничего не
распространяем. Если софт Novation не установлен, редактор работает с тем, что доказано на
приборе без таблицы — смещения имени и проверенных параметров, — и честно говорит, что
полная раскладка недоступна. Извлекатель — отдельный пункт очереди, до интерфейса.

**Смещения проверены на приборе** (09.09.2026, `docs/PATCH-PROTOCOL.ru.md` §5): база — байт
сообщения = смещение + 13, установлена структурно и подтверждена поворотом ручек; три
параметра из трёх секций (Filter1 Frequency, Osc1 Semitone, AmpEnv Attack) сошлись байт в байт
и по смещению, и по MIDI-адресу. Таблица снята с подозрения — на ней можно строить. Любой
отдельный параметр при сомнении проверяется режимом `--watch` за один поворот ручки.

## Виртуальный UltraNova: слышать патч без прибора

Запрошено владельцем 12.09: придумывать и слушать звук, когда прибора нет под рукой, а потом
отправлять готовый патч в железо. Патч при этом остаётся теми же 526 байтами, поэтому перенос
в прибор — уже решённая задача.

### Эмуляция прошивки отпадает, и это установлено, а не предположено

Образ `UNOS.syx` вскрыт полностью ([разбор](FIRMWARE.ru.md)): не зашифрован, не сжат, читается.
Но синтеза в нём **нет**. Это операционная система главного контроллера — дисплей, кнопки,
приём обновления, названия нот. Звук считает отдельный сигнальный процессор, и ни его кода, ни
волновых таблиц этот файл не содержит. Эмулировать нечего, и никакая работа с прошивкой этого
не изменит.

### Что действительно можно: свой движок по той же архитектуре

Не клон звучания, а **предпросмотр**, который читает и пишет настоящий патч. Архитектура
известна из описания параметров целиком: три осциллятора, два кольцевых модулятора, шум,
микшер на шесть источников, два фильтра с четырнадцатью типами и режимами соединения, шесть
огибающих, три LFO, матрица модуляции на двадцать слотов, цепочка эффектов, арпеджиатор,
вокодер.

**Честная граница, которую надо назвать сразу.** Совпадения звук в звук не будет: характер
прибора живёт в конкретных алгоритмах Novation, а их у нас нет. Цель — чтобы патч, собранный
вслепую, на приборе звучал **узнаваемо тем же**, а не сюрпризом. Это достижимо; побитовая
идентичность — нет, и обещать её нельзя.

### Волновые таблицы: снять с собственного прибора

Из 72 источников осциллятора 34 описываются формулой — синус, треугольник, пила, все
соотношения пилы и импульса. Остальные 36 — сэмплированные таблицы, которых у нас нет.

Путь к ним есть, и он не требует прошивки: **записать их с собственного прибора**. У UltraNova
есть USB-аудио, значит можно по очереди выставить каждую таблицу, взять ноту известной высоты
с открытыми фильтрами и без эффектов, и записать результат. Получается собственный набор
таблиц, снятый с железа владельца — тем же способом, каким снимался патч-протокол.

Это отдельная аппаратная работа, и её объём надо оценивать до того, как за неё браться.

### Очерёдность внутри задачи

1. **Движок на вычислимых формах.** Осцилляторы, микшер, фильтры, огибающие, LFO, матрица.
   Уже даёт слышимый предпросмотр для большинства патчей.
2. **Снятие волновых таблиц** с прибора и подстановка их в движок.
3. **Эффекты, арпеджиатор, вокодер** — самая трудоёмкая и наименее предсказуемая часть.
4. **Сверка с прибором.** Один и тот же патч, записанный с железа и с движка, сравнивается
   на слух и спектром. Расхождения записываются, а не заминаются.

### Что это даёт остальному продукту

Рандомизатор становится по-настоящему полезным: бросок слышен мгновенно, без прибора и без
ожидания. Библиотека получает прослушивание любого патча без железа. А редактор — возможность
работать в дороге.

## Умный рандомизатор патчей

Запрошено владельцем 12.09: получать новые звуки экспериментально. Ставится **после**
извлекателя, потому что без полного набора описаний параметров рандомизатор неизбежно
выродится в генератор шума.

**Почему обычный рандомизатор бесполезен.** Случайные значения по 497 параметрам дают
тишину, треск или сломанный патч — огибающая с нулевой атакой и нулевым сустейном не звучит,
микшер со всеми нулями молчит, а временные параметры вроде Solo вообще не принадлежат патчу.
Полезным его делают ограничения, а не случайность.

**Что значит «умный» здесь, по пунктам:**

- **Глубина, а не всё сразу.** Ручка «насколько далеко от текущего звука»: от лёгкого сдвига
  до полной неожиданности. Самый используемый режим — именно лёгкий сдвиг.
- **Область.** Рандомизировать только осцилляторы, только фильтр, только модуляцию, только
  эффекты. Остальное не трогать.
- **Замки.** Что нравится — прибивается и переживает любое количество бросков.
- **Музыкально осмысленные диапазоны.** Не `min..max` из описания, а сужённые: частота
  фильтра не в самом низу, огибающая громкости со слышимой атакой и ненулевым сустейном,
  хотя бы один источник в микшере открыт. Эти сужения — отдельная таблица поверх описаний,
  и её надо будет подбирать на слух.
- **Уважение к структуре.** Перечислимые параметры выбираются из своих значений, упакованные
  биты пишутся, не задевая соседей, временные параметры (`offset = -1`) не трогаются вовсе.
- **Воспроизводимость.** Каждый бросок имеет зерно, и зерно показывается. Удачную случайность
  можно повторить и передать словами.
- **Скрещивание и переход.** Взять два патча и смешать, или пройти между ними плавно. На
  практике это даёт больше пригодных звуков, чем чистая случайность.
- **Имя.** Сгенерированному патчу — сгенерированное имя, иначе библиотека зарастёт
  одинаковыми строками.

**Что уже готово под это.** Черновик (`PatchModel`) хранит исходник рядом с правкой, умеет
отмену и сравнение — значит неудачный бросок откатывается одним движением, а удачный видно
по списку изменившихся смещений. Кодек (`ParameterCodec`) уже знает про диапазоны, знаковые
значения, упакованные биты и перечисления. Писать надо только слой ограничений и сам бросок.

**Границы.** Только буфер редактирования, во флеш ничего. Рандомизатор не должен уметь
портить сохранённые патчи.

## Волновые таблицы: свои загрузить нельзя

Проверено 12.09 по описанию параметров и по разобранному протоколу.

У осциллятора **72 источника**: 34 именованные формы (от синуса и пилы до `EPiano Bell`,
`Clavinova`, `String Machine` и `Digital Pad`), затем `WaveTable1`..`WaveTable36`, и наконец
`AudioIn L` и `AudioIn R`.

**36 волновых таблиц зашиты в прошивку.** Команды передачи волновой таблицы в разобранном
протоколе нет ни одной: всё, что в родном софте называется upload, относится к **патчам**
(`WinLibrarian.exe /PatchUploader`, заводской загрузчик патчей). Единственное место, где
таблицы могли бы лежать, — образ прошивки `UNOS.syx`, а он упакован 8:7 и имеет высокую
энтропию, то есть сжат или зашифрован. Подменять прошивку ради своих таблиц — не тот путь,
которым стоит идти.

**Зато свой звук в прибор завести можно другим способом.** `AudioIn L` и `AudioIn R`
выбираются как источник осциллятора наравне с таблицами, а `Ext In` есть отдельным входом
микшера. То есть внешний сигнал проходит через фильтры, огибающие, модуляцию и эффекты
синтезатора — этим же путём работает вокодер. Для редактора это значит, что выбор источника
осциллятора должен показывать аудиовходы как равноправные пункты, а не прятать их в конце
списка.

## Библиотека

- Папки/коллекции, поиск по имени, категории и жанру, избранное, сортировка и
  операции с несколькими патчами. Пользовательские отметки хранятся отдельно от
  ограниченных метаданных аппаратного патча.
- Импорт/экспорт `.syx`, затем совместимость с библиотеками `.nun`.
- Прослушивание через загрузку в оперативный edit buffer с сохранением локального
  черновика. Запись в постоянную память выполняется отдельной командой с банком и слотом.
- Чтение и резервное копирование банков A–D; понятный прогресс и обработка обрыва связи.
- Просмотр, организация и редактирование локальных файлов работают без подключённого
  синтезатора и во время работы родного VST. Прослушивание через прибор требует
  свободного доступа к его текущему звуку.

## Совместная работа с родным софтом

У UltraNova один текущий edit buffer. Разные MIDI-порты сами по себе не делают
одновременную запись двух редакторов согласованной. Рабочее решение для первой версии:
приоритет родного редактора при его загрузке; наше приложение продолжает работать
с локальной библиотекой и черновиками.

| Сценарий | Ожидаемое поведение нашего приложения |
|---|---|
| Самостоятельная работа | Редактор управляет синтезом, библиотека умеет читать и прослушивать патчи |
| Ableton запущен без родного VST | Сам факт запуска DAW не отключает наш редактор |
| Родной VST загружен до запуска нашего приложения | Библиотека и локальное редактирование доступны; запись в прибор приостановлена |
| Родной VST загружается во время нашей работы | Аппаратные операции приостанавливаются, неотправленные изменения снимаются с очереди, черновик сохраняется |
| Родной VST выгружен | Повторное обнаружение и чтение текущего звука; старый черновик не отправляется автоматически |
| Запущен родной Automap Server | Наш Automap уступает управление дисплеем и лампами; библиотека остаётся доступна |
| USB отключён и подключён снова | Локальная работа продолжается; соединение создаётся заново и состояние перечитывается |

В интерфейсе показывается причина паузы: например, «Синтезатором управляет Novation
Editor». Закрытие окна VST не обязательно выгружает экземпляр плагина: состояние
определяется по экземпляру, а не по видимости окна или фокусу Ableton.

### Основания и ограничения

- Подробный разбор `editor-native-notes.md` показывает загрузку Novation HIDI и
  Automap Client, а также отдельный фильтр MIDI-событий VST-хоста. Ранний вывод
  «редактор работает исключительно через MIDI DAW» неверен. Librarian также использует HIDI.
- В конструкторе VST найден `NovaControlPluginHardwareLock`; его наличие при работе
  установленного плагина уже подтверждено. Полная совместимость ещё требует приёмки.
  Наше приложение не создаёт и не захватывает этот именованный объект: его наличие
  может заставить официальный плагин показать «Already Open».
- Для родного Automap исследован `AutomapServerRunning`. Обнаружение редактора
  и обнаружение владельца панели — отдельные проверки. Родной VST сам по себе
  не равнозначен запуску родного Automap Server.
- Приватные KS-пины 6/10 подтверждены перечислением устройства, но совместимость
  нашего обмена через них с HIDI пока не подтверждена. Автоматического перехода на
  публичные пины 4/8 при неудаче не предусматривается.
- Успешное открытие порта не доказывает отсутствие другого управляющего процесса.
  Также опрос мьютекса по таймеру не гарантирует отсутствие гонки в момент старта VST.
  Проверка перед отправкой, отмена очереди и корректное освобождение транспорта
  должны проверяться вместе на железе.
- Аппаратные операции библиотекаря, включая прослушивание и Compare, подчиняются
  тому же доступу, что и редактор. Пока родной софт управляет звуком, фоновые
  запросы, смена патча, запись метаданных и воспроизведение старых команд отключены.
- Работа Automap с отдельным выходом в DAW должна сохраняться, если родной софт
  не занял панель. Сохранность этой работы при передаче доступа к редактору требует теста.
- Маршрутизация нот настраивается отдельно. Нельзя требовать отключить штатный
  MIDI-порт UltraNova в Ableton как условие совместимости; также нужно исключить
  двойные ноты при одновременном использовании прямого входа и loopMIDI.

Наше приложение не меняет установку Novation, драйверы и защиту памяти, не завершает
родные процессы. Безопасное совместное редактирование обоими интерфейсами одновременно
не заявляется до проверки доставки уведомлений и согласования состояния.

## Устройство реализации

Модель патча и локальная библиотека не зависят от аппаратного соединения. Состояние
доступа к прибору хранится централизованно; редактор и библиотекарь используют одну
очередь аппаратных команд. Сессия Automap управляется отдельно.

Состояния аппаратного редактора: отключён, подключается, синхронизируется, готов,
управляется родным софтом, ошибка. Изменения, полученные с прибора, не считаются
командами на отправку. При смене сессии устаревшие команды отбрасываются.

## Очерёдность и приёмка

1. ~~Восстановить сборку~~ — **сделано, выпущено как 1.2.1** (2026-09-09). Движок
   восстановлен из уцелевшей сборки, приватный пин 6 берётся первым, завершение через
   `CancelIoEx` и упорядоченное закрытие пинов; всё это проверено на приборе
   (`RESTORE-1.2.1.md`, `CHANGELOG.md`).
2. Проверить доступ к текущему патчу и сосуществование с родным VST/Librarian.
   **Уже проверено на приборе:** прямой вход `UltraNova` остаётся свободен при нашей
   работе; загрузка и выгрузка плагина на треке — пересылка встаёт на паузу и
   возобновляется, мьютекс `NovaControlPluginHardwareLock` виден снаружи; закрытие нашего
   приложения не роняет MIDI-входы Ableton; прибор сам присылает буфер редактирования при
   смене патча. **Ещё не проверено:** оба порядка запуска (плагин раньше нас и наоборот),
   загрузка проекта Ableton с сохранённым состоянием VST, скрытое окно плагина,
   переподключение USB, отсутствие неожиданных смен патча. Двойные ноты и колёса при
   загруженном плагине сняты автоматической паузой пересылки; двойные колёса по
   назначениям, отличным от заводских, — сознательно не подавляются, это назначения
   пользователя.
3. Первый полезный выпуск: открытие `.syx`, чтение текущего патча, редактирование
   осцилляторов/фильтров и сохранение в локальную библиотеку без запуска DAW/VST.
   **Готово из этого (09.09.2026, без интерфейса):** транспорт по приватной паре 6/10 с
   чтением и записью, доказанный на приборе в обе стороны; протокол как чистые функции;
   модель патча с отменой и сравнением; чтение и запись `.syx` одиночных и банковых;
   сессия, держащая черновик в шаге с прибором по правилам выше (грязный черновик не
   затирается, ответы прибора не эхом, слежение за панелью опросом с задержкой).
   **Не начато:** извлекатель таблицы, модель параметров поверх таблицы, интерфейс.
4. Расширить редактор на остальные разделы синтеза, подключить к UI уже имеющиеся Undo/Redo и diff для Compare;
   расширить библиотеку на коллекции, `.nun`, банки и резервное копирование.
5. Проверить полный цикл «выбрал звук → изменил → сравнил → сохранил → открыл снова»
   и передачу управления родному софту. Выпускать после проверки на подключённой UltraNova.

Состояние на 2026-09-11: сборка успешна, 0 ошибок и 2 прежних предупреждения CS9057;
**100/100 регрессионных проверок (число растёт каждый заход — верить выводу раннера)**. Ядро редактора уже существует. Добавлены
централизованная проверка владельца перед отправками, состояния/таймаут синхронизации,
сохранение флагов метаданных и отмена запросов старого поколения соединения.
Исправлены подмена черновика банковым ответом и устаревшие данные NRPN.
Тесты используют подставной транспорт: изменения 11.09 на приборе не проверены.
Локальный извлекатель, семантическая модель параметров, редакторский UI, интеграция
DeviceWatcher с reconnect и арбитраж панели Automap ещё не сделаны.
Совместимость из п. 2 частично проверена ранее; остальные сценарии остаются условиями
приёмки. Изменения рабочего дерева не являются новым релизом.
